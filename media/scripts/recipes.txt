/* Only is called base until pz fixes the bug that requires the module to be called Base. */
/* module CarBombs { */
module Base {
	craftRecipe MakeAcceleratorBomb {
		tags = AnySurfaceCraft;RightClickOnly,
		/* pipe bomb, crafted trigger, scrap electronics, wire, tape? */
		inputs {
		},
		outputs {
			/* convert this to a mapper? */
			item 1 Base.AcceleratorBomb
		}
		category = Electrical,
		skillrequired = Electricity:2;Mechanics:4,
	}
}